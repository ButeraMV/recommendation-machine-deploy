import React, { Component } from 'react';

class Header extends Component {
  render() {
    return (
      <h1 className="site-title">Recommendation Machine</h1>
    );
  }
}

export default Header;

